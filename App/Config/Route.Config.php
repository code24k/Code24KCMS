<?php
return [
    /**
     * Routing rules: Currently, pseudo-static routing is not supported; only parametric routing is supported
     * Optional parameter: ParamterRoute RewriteRoute(X)
     * 路由规则，目前不支持伪静态，仅支持参数路由
     * 可选参数: ParamterRoute RewriteRoute(X)
     */
    "Default" => "ParamterRoute",
];
?>